[Service]
# Must run before waydroid-config-hammerhead's 10-hammerhead-config.conf so
# config_android exists before the generated LXC include list is refreshed.
ExecStartPre=/usr/libexec/waydroid-android-hammerhead/prepare --ensure
